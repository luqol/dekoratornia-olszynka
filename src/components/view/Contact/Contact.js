import styles from './Contact.module.scss';

const Contact = () => {
    return(
        <div className={styles.contact}>
            <h2>Contact</h2>
        </div>
    );
};

export default Contact;